# --- secrets -----------------------------------------------------------------
.env
.env.local
.env.*.local
!.env.example

# --- python ------------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage

# --- node / next -------------------------------------------------------------
node_modules/
.next/
out/
.vercel/
*.tsbuildinfo
next-env.d.ts

# --- engine caches -----------------------------------------------------------
engine/.cache/

# --- research outputs (regenerated by `make paper`, never committed) ----------
paper-exports/*
!paper-exports/.gitkeep

# --- agent / assistant scratch (deliberately never published) -----------------
# This is a research repository: the paper reports the method, and how the code
# was written is not part of the method. Kept out of the published history on
# purpose, not by accident — a machine-local ignore file does not travel with a
# clone, so the rule belongs here.
CLAUDE.md
**/CLAUDE.md
.claude/

# --- os ----------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
